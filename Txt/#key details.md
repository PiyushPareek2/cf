\#key details\*use  median for min  operation to make all element in array equal by adding or sub any number\*to check key x exist in map if we use mp\[x]==0 gives incorrect and as it may have assigned to 0 instead use mp.count(x) it returns 1 if already exists otherwise zero\*map m.size() give size of map all key value even one of those which you never created but created when you tried to access them to get rid from always delete using m.erase(key)\* agar return long long aur int k, sum then return k\*sum can cause overflow therefore use return 1LL \*k\*sum\* ek string me baar baar pop front ke jagha sirf do varible se store kar sakte ha starting\_index and size  //useful in sliding window\* agar kahi interval ha to sort to kar le ek  baar kuch to hoga hi  greedy whagra  2 question n meeting in one room and non overlapping intervals  taki pata rahe st ko deakhte hue sort karna ha ya end koagar interval merge to start ke hisab se sort or agar sirf non overlapping to end ke hisab se sort\* comparator   kya a b ke agge sahi ha&#x09;					sort(arr.begin(), arr.end(), \[](const auto\& a, const auto\& b) {&#x20;  						 return a\[1] < b\[1]; // sort by end time&#x09;								});&#x09;				        sort(arr.begin(), arr.end(), \[\&freq](const auto\& a, const auto\& b) {&#x20;                                                if (freq\[a] != freq\[b])&#x20;                                                return freq\[a] < freq\[b];&#x20;                                                      return a < b;                                                                      });#### \###### state object matlab ek transition se dusri par jane par kya kya info chaiye ya kaam aayege##### \* MOVING i INSIDE A NESTED LOOP AROMATIZED O(N) -----> " WHAT INFORMATION I REALLY NEED TO KNOW? or what state/object do i need to maintain"##### If I had to pause the algorithm and resume later, what is the smallest information packet I need to save?Interval merging ka code  interval sorted honi chaiye              for(auto\& interval : intervals){&#x20;					   if(ans.empty() || interval\[0] > ans.back()\[1]){&#x20;       					ans.push\_back(interval);&#x20;   							}&#x20;   					   else{&#x20;                                       ans.back()\[1] = max(ans.back()\[1], interval\[1]);&#x20;                                         }&#x20;                                          }\# return ans% 1e9 +7                 declare int mod= 1e9 + 1;&#x09;				aur jaha par bhi ans accumulate ho raha ha  ans=(ans+something)%mod;\~ bhi ek gate ha compliment kar deta ha 11011 -> 00100     5bit he man int ko&#x20;# condition reduction think about all the condition you have -> label with their result -> merge conditions with same result&#x20; if(word.find(s)!=string::npos)ans++; agar s word ki substring to ans++\# cycle exists in undirected graph if neigh of a node is already visited and is not a parent of it  non parent visited neighbor\# bfs is a special case of dijsktra when every time reaching a new node cost a constant value 1 or k&#x09;so if min operation question if each operation cost is same throughout implies bfs (since min cost return ans as bfs reach)&#x09; and if operation cost is variable  then use dijsktra